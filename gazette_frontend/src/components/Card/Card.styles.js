import styled from "styled-components";

export const CardContainer = styled.div`
  font-size: 15px;

  p {
    color: #111;
    font-size: 1em;
    margin: 0 0 1.25rem;
    text-transform: uppercase;
    position: relative;
    font-weight: 500;
  }
`;
